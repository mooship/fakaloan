export enum LanguageCode {
  English = 'en',
  Zulu = 'zu',
  Xhosa = 'xh',
}

export enum SubscriptionStatus {
  Active = 'active',
  Trialing = 'trialing',
  PastDue = 'past_due',
  Canceled = 'canceled',
  NonRenewing = 'non-renewing',
  Attention = 'attention',
}

export enum Theme {
  Light = 'light',
  Dark = 'dark',
  Auto = 'auto',
}
