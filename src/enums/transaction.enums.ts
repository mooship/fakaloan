/**
 * transaction.enums.ts
 *
 * Enum for transaction types in Fakaloan.
 * Used to distinguish between credit and repayment transactions.
 *
 * @module enums/transaction.enums
 * @enum {string}
 */
export enum TransactionTypeEnum {
  Credit = 'credit',
  Repayment = 'repayment',
}
